CREATE TABLE `artale_mob_droplist` (
  `mob_id` int NOT NULL,
  `equipment_id` int NOT NULL
) 


/* Most likely not the best way but the idea is to map the mob_id with an equipment_id drop and repeat boss_id multiple times */