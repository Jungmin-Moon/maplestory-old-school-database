CREATE TABLE `artale_boss_droplist` (
  `boss_id` int NOT NULL,
  `equipment_id` int NOT NULL
) 

/* Most likely not the best way but the idea is to map the boss_id with an equipment_id drop and repeat boss_id multiple times */